<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> Erro ao gravar esta Categoria:</h2>

      <ul>
      <% @category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Categoria %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Gravar Categoria' %>
  </div>
<% end %>
